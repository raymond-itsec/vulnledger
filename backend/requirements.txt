fastapi==0.136.1
uvicorn[standard]==0.47.0
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0
alembic==1.18.4
pydantic[email]==2.13.4
pydantic-settings==2.14.0
bcrypt==5.0.0
zxcvbn==4.5.0
python-multipart==0.0.27
pyyaml==6.0.3
mistune==3.2.1
minio==7.2.20
weasyprint==68.1
pydyf==0.12.1
mailjet-rest==1.6.0
slowapi==0.1.9
authlib==1.7.2
# httpx pinned explicitly because authlib uses it for OIDC discovery + token exchange.
# Not imported directly by application code.
httpx==0.28.1
clamd==1.0.2
python-json-logger==4.1.0
# Prometheus metrics. Used by app/middleware/metrics.py to expose
# /api/v1/metrics for vmagent (lands with the future observability
# stack). Single-process mode today; switch to multiprocess mode when
# we run >1 uvicorn worker per container.
prometheus-client==0.25.0
